# Capabilities

```bash
python -m a11yway.main --capabilities
```

The command reports which optional adapters and libraries are available in the current environment. Missing optional tools disable only the related module. Static checks remain usable.

Status values are `available_verified`, `available_untested`, `unavailable`, and `unsupported_on_current_platform`.
