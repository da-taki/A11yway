# Component Testing

`--components` inspects complex ARIA component patterns such as dialogs, tabs, comboboxes, menus, sliders, tree items, duplicate IDs, and carousels.

It checks names, required states, ID uniqueness, and pause-control evidence. It uses WAI-ARIA pattern guidance as review evidence, not certification. Keyboard interaction must still be verified with browser or workflow tests.
