# Optional dependency for A11yway browser interaction mode.
# Static audits work without this file.
#
# Install with:
#   pip install -r requirements-browser.txt
#   python -m playwright install chromium
playwright
